<div class="modal fade" id="modalAddSlot" tabindex="-1" role="dialog" aria-labelledby="addSlotModalLabel"
     aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}, 'action': path('schedule_slot_insert')}) }}
            <div class="modal-header">
                <h5 class="modal-title" id="addSlotModalLabel">Add Slot?</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body">
                {{ include('schedule/modal/_body_add_slot.html.twig', { 'form': form }) }}
            </div>

            <div class="modal-footer">
                <span class="text-danger form_error"></span>
                <button type="button" class="btn btn-light" data-dismiss="modal">Close</button>
                <button class="btn btn-success">Save new slot</button>
            </div>
            {{ form_end(form) }}

            <script src="{{ asset('build/add_slot.js') }}"></script>
        </div>
    </div>
</div>
